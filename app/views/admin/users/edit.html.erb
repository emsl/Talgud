<h1><%= t('.title') %></h1>

<% semantic_form_for @user, :url => admin_user_path(@user) do |f| %>
  <%= render 'form' %>
  <%= f.buttons %>
<% end %>

<h2><%= t('.roles') %></h2>

<%= render :partial => 'shared/object_roles' %>