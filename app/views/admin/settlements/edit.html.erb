<h1><%= t('.title') %></h1>

<% semantic_form_for @settlement, :url => admin_county_municipality_settlement_path(@county, @municipality, @settlement) do |f| %>
  <%= render 'form' %>
  <%= f.buttons %>
<% end %>